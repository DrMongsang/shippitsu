# 🔮 SHARINGAN ENGINE セットアップガイド

毎日12:00に自動実行される来店件数予測システムのセットアップ手順です。

**所要時間**: 約10分
**初回のみの作業**: すべて
**以降**: 完全自動、認証不要

---

## 📋 目次

1. [サービスアカウントの作成](#1-サービスアカウントの作成)
2. [スプレッドシートに権限付与](#2-スプレッドシートに権限付与)
3. [GitHub Secretsの設定](#3-github-secretsの設定)
4. [動作確認](#4-動作確認)

---

## 1. サービスアカウントの作成

### 1-1. Google Cloud Platformにアクセス

1. https://console.cloud.google.com/ にアクセス
2. Googleアカウントでログイン
3. プロジェクトを選択（または新規作成）

### 1-2. Google Sheets APIを有効化

1. 左上のメニュー ≡ をクリック
2. 「APIとサービス」→「ライブラリ」を選択
3. 検索バーに「Google Sheets API」と入力
4. 「Google Sheets API」をクリック
5. 「有効にする」ボタンをクリック

### 1-3. サービスアカウントを作成

1. 左上のメニュー ≡ をクリック
2. 「APIとサービス」→「認証情報」を選択
3. 上部の「+ 認証情報を作成」ボタンをクリック
4. 「サービスアカウント」を選択
5. 以下を入力：
   - **サービスアカウント名**: `sharingan-engine`（任意）
   - **サービスアカウントID**: 自動入力される
   - **説明**: `来店件数予測システム用`（任意）
6. 「作成して続行」をクリック
7. 「ロール」は設定不要、「続行」をクリック
8. 「完了」をクリック

### 1-4. JSONキーをダウンロード

1. 作成したサービスアカウント（例: `sharingan-engine@...`）をクリック
2. 上部の「キー」タブをクリック
3. 「鍵を追加」→「新しい鍵を作成」を選択
4. 「JSON」を選択して「作成」をクリック
5. **JSONファイルが自動ダウンロードされます（重要！）**
   - ファイル名例: `your-project-123456-abcdef123456.json`
   - **このファイルを安全に保管してください**

### 1-5. サービスアカウントのメールアドレスをコピー

1. サービスアカウントのページに表示されているメールアドレスをコピー
   - 形式: `sharingan-engine@your-project.iam.gserviceaccount.com`
   - **このメールアドレスは次のステップで使用します**

---

## 2. スプレッドシートに権限付与

### 2-1. 予約データスプレッドシート

1. https://docs.google.com/spreadsheets/d/18L7Pv605gE33CUHaDayG4FdDIMSBlV7dDpuGgcjDk8I を開く
2. 右上の「共有」ボタンをクリック
3. **Step 1-5でコピーしたサービスアカウントのメールアドレス**を入力
4. 権限を「編集者」に設定
5. 「送信」をクリック（通知メールは不要なのでチェックを外してOK）

### 2-2. 予約ステータススプレッドシート

1. https://docs.google.com/spreadsheets/d/1aIgHCT88h4dmmGTFcdpl5-BydqblXt0PNg0_UM_IQKU を開く
2. 右上の「共有」ボタンをクリック
3. **Step 1-5でコピーしたサービスアカウントのメールアドレス**を入力
4. 権限を「閲覧者」に設定（読み取り専用でOK）
5. 「送信」をクリック

✅ これで、サービスアカウントがスプレッドシートにアクセスできるようになりました！

---

## 3. GitHub Secretsの設定

### 3-1. JSONファイルの内容をコピー

1. Step 1-4でダウンロードしたJSONファイルをテキストエディタで開く
2. **ファイル全体**をコピー（改行含む全て）
   ```json
   {
     "type": "service_account",
     "project_id": "your-project",
     "private_key_id": "...",
     ...
   }
   ```

### 3-2. GitHub Secretsに登録

1. このリポジトリのページ（https://github.com/DrMongsang/shippitsu）を開く
2. 上部メニューの「Settings」をクリック
3. 左サイドバーの「Secrets and variables」→「Actions」を選択
4. 「New repository secret」ボタンをクリック
5. 以下を入力：
   - **Name**: `GOOGLE_CREDENTIALS`（大文字小文字を正確に）
   - **Secret**: Step 3-1でコピーしたJSONファイルの内容を貼り付け
6. 「Add secret」をクリック

✅ これで、GitHub ActionsがGoogle スプレッドシートにアクセスできるようになりました！

---

## 4. 動作確認

### 4-1. 手動でワークフローを実行

1. このリポジトリの「Actions」タブをクリック
2. 左サイドバーの「Daily Visitor Prediction」を選択
3. 右上の「Run workflow」ボタンをクリック
4. ブランチを選択（通常は `main` または現在のブランチ）
5. 「Run workflow」をクリック

### 4-2. 実行結果を確認

1. 実行中のワークフローが表示されます
2. ワークフロー名をクリックして詳細を表示
3. 各ステップの実行状況を確認
   - ✅ が表示されれば成功
   - ❌ が表示された場合はエラーログを確認

### 4-3. スプレッドシートを確認

1. https://docs.google.com/spreadsheets/d/18L7Pv605gE33CUHaDayG4FdDIMSBlV7dDpuGgcjDk8I を開く
2. 「未来_2W」シートと「未来_2M」シートに予測結果が出力されていることを確認

---

## 🎉 完了！

これで以下が実現されました：

✅ **毎日12:00（日本時間）に自動実行**
✅ **認証不要で完全自動化**
✅ **スプレッドシートに予測結果を自動出力**

---

## 📅 自動実行スケジュール

- **実行時刻**: 毎日 12:00（日本時間）
- **実行内容**:
  1. スプレッドシートから予約データを取得
  2. 機械学習モデルで来店件数を予測
  3. 2週間と2ヶ月の予測結果をスプレッドシートに出力

---

## 🔧 カスタマイズ

### 実行時刻を変更したい場合

`.github/workflows/daily_prediction.yml` の以下の行を編集：

```yaml
schedule:
  - cron: '0 3 * * *'  # 現在: 毎日 UTC 03:00 = JST 12:00
```

**日本時間とUTCの対応表**:
- JST 09:00 → UTC 00:00 → `'0 0 * * *'`
- JST 12:00 → UTC 03:00 → `'0 3 * * *'` ← 現在の設定
- JST 15:00 → UTC 06:00 → `'0 6 * * *'`
- JST 18:00 → UTC 09:00 → `'0 9 * * *'`

変更後、ファイルをコミット＆プッシュすれば反映されます。

---

## ❓ トラブルシューティング

### エラー: `❌ 環境変数 GOOGLE_CREDENTIALS が設定されていません`

→ GitHub Secretsの設定を確認してください（Step 3）

### エラー: `❌ スプレッドシート読み込みエラー`

→ サービスアカウントに権限が付与されているか確認してください（Step 2）

### エラー: `gspread.exceptions.APIError: [403] ... PERMISSION_DENIED`

→ スプレッドシートの共有設定を確認してください（Step 2）

### GitHub Actionsが実行されない

1. 「Actions」タブで実行履歴を確認
2. リポジトリの「Settings」→「Actions」→「General」で、Actionsが有効化されているか確認
3. ワークフローファイル（`.github/workflows/daily_prediction.yml`）が正しくプッシュされているか確認

---

## 📞 サポート

問題が解決しない場合は、GitHub Issuesで報告してください。

---

## 🔐 セキュリティに関する注意

- **JSONキーファイルは絶対に公開しないでください**
- GitHub Secretsに保存されたデータは暗号化されています
- サービスアカウントには最小限の権限のみを付与してください
- 不要になったサービスアカウントは削除してください

---

**作成日**: 2024年11月29日
**バージョン**: 1.0
